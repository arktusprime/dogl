collab LayoutProcess
    == MainPool
        -- Ops
            || Default
                (s) Start
                    => Review
                [] Review
                    => Route
                <x> Route
                    => Done
                (e) Done

layout
    == MainPool {0 0 600 320}
        -- Ops {0 40 600 80}
            || Default {120 0 180 320}
                (s) Start {80 140 36 36}
                [] Review {180 132 100 52}
                <x> Route {340 136 50 50}
                (e) Done {460 140 36 36}
