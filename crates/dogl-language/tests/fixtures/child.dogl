collab ChildProcess
    == MainPool
        -- Ops
            || Default
                (s) Start
                    => Finish
                [] Finish
                    => EndNode
                (e) EndNode
