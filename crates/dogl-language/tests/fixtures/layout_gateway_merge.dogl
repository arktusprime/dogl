collab GatewayMerge
    == MainPool
        -- Ops
            || Default
                (s) StartA
                    => Merge
                (s) StartB
                    => Merge
                <x> Merge
                    => Done
                (e) Done
