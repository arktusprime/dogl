collab CommandProcess
    == MainPool
        -- Ops
            || Default
                [] ReviewOrder
                    [do.exec] validateOrder(order.id)
                    => Done
                (e) Done
